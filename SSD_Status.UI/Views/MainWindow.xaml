<Window x:Class="SSD_Status.WPF.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SSD_Status.WPF.Views"
        mc:Ignorable="d"
        Title="SSD Status" Height="500" Width="700">
    <Grid>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <local:DriveInfo DataContext="{Binding DriveInfo}"/>
            <local:RawValueInfo DataContext="{Binding RawValueInfo}" Grid.Column="1"/>
            <local:UsageStatsInfo DataContext="{Binding UsageStatsInfo}" Grid.Row="1" Grid.ColumnSpan="2"/>
        </Grid>
    </Grid>
</Window>
